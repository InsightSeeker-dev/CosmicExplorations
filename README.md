# CosmicExplorations
Application Android native (Kotlin + Jetpack Compose) utilisant l'API NASA APOD. Implémentation stricte d'une Clean Architecture (MVVM, Repository Pattern) avec gestion Offline-first (Room) et intégration Firebase
